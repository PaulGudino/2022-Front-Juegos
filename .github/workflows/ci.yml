name: CI

on:
  push:
    branches:
      - continuous_integration_dev
  pull_request:
    branches:
      - continuous_integration_dev

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '14'

    - name: Install dependencies
      run: npm install

    - name: Build application
      run: npm run build -- --prod
